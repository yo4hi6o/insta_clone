enum UploadType {
  GALLERY,
  CAMERA,
}

enum PostCaptionOpenMode {
  FROM_POST,
  FROM_FEED,
}

enum FeedMode {
  FROM_FEED, //自分+フォローしているユーザー
  FROM_PROFILE, //プロフィール画面に表示されているユーザーのみ
}

enum PostMenu {
  EDIT,
  DELETE,
  SHARE,
}