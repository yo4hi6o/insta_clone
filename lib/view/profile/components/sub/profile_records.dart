import 'package:flutter/material.dart';

class ProfileRecords extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("ProfileRecords"),
      color: Colors.yellow,
    );
  }
}
